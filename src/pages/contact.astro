---
import Layout from "../layouts/Layout.astro";
---

<Layout title="About - Honey-Do Heroes">
  <style>
    form {
      gap: 10px;
      width: 70%;
      margin: 20px auto;

      label {
        font-size: 1.2rem;
      }

      input,
      textarea {
        border-radius: 5px;
        min-height: 30px;
        padding: 5px 10px;
        color: #000;
      }

      button {
        margin: 10px auto;
        padding: 10px 50px;
        width: fit-content;
        border: 1px solid #fff;
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(255, 255, 255, 0.5);
        transition: all 0.3s ease;
        &:hover {
          font-weight: bold;
          box-shadow: 0 0 20px rgba(255, 255, 255, 0.8);
        }
      }
    }
  </style>
  <section class="max-w-4xl w-full p-8 mt-10 rounded-xl bg-gray-800 shadow-lg">
    <h1 class="text-3xl font-bold mb-4 text-center text-blue-400">
      Contact Us
    </h1>
    <p class="text-center text-lg text-gray-300">
      We’d love to hear from you! Whether you need a repair, a setup, or just
      have a question about our services, send us a message below. You can also
      leave us a comment about the site.
    </p>

    <form
      name="contact"
      method="POST"
      data-netlify="true"
      netlify-honeypot="bot-field"
      class="flex flex-col"
    >
      <input type="hidden" name="form-name" value="contact" />
      <p style="display:none">
        <label>Don’t fill this out: <input name="bot-field" /></label>
      </p>

      <label for="name">Name</label>
      <input id="name" type="text" name="name" required />

      <label for="email">Email</label>
      <input id="email" type="email" name="email" required />

      <label for="message">Message / Comment</label>
      <textarea id="message" name="message" rows="5" required></textarea>

      <button type="submit">Send</button>
    </form>

    <p>
      Or call us directly: <a href="tel:+15162204640" class="underline"
        >(516) 220-4640</a>
    </p>
  </section>
</Layout>
